import React from 'react';
import PropTypes from 'prop-types';

import './Label.scss';

const Label = () => (
  <label className="required">
    Название
  </label>
);

Label.propTypes = {
};

export {Label};